/***
*
*
*
*
*
***/

var context = nlapiGetContext();

var webstore = {
	afterSubmit : function(type){

		//after the order has been submitted
		if(type == 'create'){

		var newId = nlapiGetRecordId();
		var newType = nlapiGetRecordType()

		//load the old record


		// get the lines and deterimine what new cusotmers we need to create



		//


		}

	}
}